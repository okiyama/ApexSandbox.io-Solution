public static Integer findLargest(Integer num1, Integer num2, Integer num3) {
	return Math.max(Math.max(num1, num2), num3); 
    //Can this be a generic function? Would need argument expansion of some sort
}