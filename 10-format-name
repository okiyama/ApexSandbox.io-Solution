public String formatName(String firstName, String lastName) {
    String toRet = '';
    toRet += lastName != null ? lastName : '';
    toRet += (firstName != null && toRet != '') ? ', ' + firstName : firstName != null ? firstName : '';
    return toRet;
}